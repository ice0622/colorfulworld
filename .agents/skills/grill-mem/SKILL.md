---
name: grill-mem
description: A relentless interview to sharpen a plan or design, backed by persistent memory of past investigations and decisions.
disable-model-invocation: true
---

Run a `/grilling` session, using the `/domain-modeling` and `/codebase-memory` skills.

Before asking the first question, follow the LOAD procedure in `/codebase-memory`.
After the grilling session concludes, follow the COMPACT procedure in `/codebase-memory`.
